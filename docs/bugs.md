# Bug reports
```md
fix : Renderer doesn't save `r_mode -1`. It works after vid_restart, but switches back to `r_mode -2` on engine reload
fix : CQ3 w-only movement might be acting weird after the port baseq3a
```
